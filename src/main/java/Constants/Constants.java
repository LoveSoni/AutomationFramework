package Constants;

import com.sun.tools.internal.jxc.ap.Const;

import java.io.File;

public class Constants {
    public String slash = File.separator;

    public String DEALER_APP_PATH = "Users" + slash + System.getProperty("user.name") + slash + "documents" + slash + "builds" + "catalogv2.apk";

}
